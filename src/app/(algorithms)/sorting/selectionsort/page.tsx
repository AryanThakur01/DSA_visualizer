import SelectionSort from "@/components/algorithmComps/sorting/selectionSort/SelectionSort";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return <SelectionSort />;
};

export default page;
